name: GMGN DOMPurify version trace

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Extract bundled DOMPurify
        run: python gmgn_dompurify_actual_locator.py

      - name: Upload DOMPurify module
        uses: actions/upload-artifact@v4
        with:
          name: gmgn-dompurify-module
          path: gmgn_dompurify_module.json
          if-no-files-found: error
          retention-days: 3
