name: GMGN Html component call-site trace

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Trace Html component call sites
        run: python gmgn_html_component_callsites.py

      - name: Upload call-site report
        uses: actions/upload-artifact@v4
        with:
          name: gmgn-html-component-callsites
          path: gmgn_html_component_callsites.json
          if-no-files-found: error
          retention-days: 3
