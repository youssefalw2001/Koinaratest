name: GMGN historical community media survey

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  survey:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Paginate historical public media
        run: python gmgn_historical_media_survey.py

      - name: Upload historical media survey
        uses: actions/upload-artifact@v4
        with:
          name: gmgn-historical-media-survey
          path: gmgn_historical_media_survey.json
          if-no-files-found: error
          retention-days: 3
