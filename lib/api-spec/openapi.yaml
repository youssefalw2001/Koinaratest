openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.2.0
  description: Koinara API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: users
    description: User operations
  - name: predictions
    description: Prediction operations
  - name: quests
    description: Quest/Earn Center operations
  - name: rewards
    description: Daily rewards and bonuses
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /users/register:
    post:
      operationId: registerUser
      tags: [users]
      summary: Register or login a user via Telegram ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterUserBody"
      responses:
        "200":
          description: User registered or existing user returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /users/{telegramId}:
    get:
      operationId: getUser
      tags: [users]
      summary: Get user by Telegram ID
      parameters:
        - in: path
          name: telegramId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "404":
          description: User not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/{telegramId}/stats:
    get:
      operationId: getUserStats
      tags: [users]
      summary: Get user stats (win rate, profit, streak, referrals)
      parameters:
        - in: path
          name: telegramId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserStats"

  /users/{telegramId}/wallet:
    patch:
      operationId: updateWallet
      tags: [users]
      summary: Link TON wallet address to user
      parameters:
        - in: path
          name: telegramId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWalletBody"
      responses:
        "200":
          description: Wallet updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /users/{telegramId}/vip:
    post:
      operationId: upgradeToVip
      tags: [users]
      summary: Upgrade user to VIP (deduct TC or record TON payment)
      parameters:
        - in: path
          name: telegramId
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpgradeToVipBody"
      responses:
        "200":
          description: VIP status granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Insufficient credits or already VIP
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /predictions:
    post:
      operationId: createPrediction
      tags: [predictions]
      summary: Place a trade prediction (Long or Short) — deducts Trade Credits
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePredictionBody"
      responses:
        "201":
          description: Prediction created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Prediction"
        "400":
          description: Insufficient Trade Credits or validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /predictions/{id}/resolve:
    post:
      operationId: resolvePrediction
      tags: [predictions]
      summary: Resolve a prediction — awards Gold Coins on win
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolvePredictionBody"
      responses:
        "200":
          description: Prediction resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Prediction"

  /predictions/user/{telegramId}:
    get:
      operationId: getUserPredictions
      tags: [predictions]
      summary: Get recent predictions for a user
      parameters:
        - in: path
          name: telegramId
          required: true
          schema:
            type: string
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of predictions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Prediction"

  /predictions/leaderboard:
    get:
      operationId: getLeaderboard
      tags: [predictions]
      summary: Get top players by Gold Coins earned
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: Leaderboard
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaderboardEntry"

  /quests:
    get:
      operationId: listQuests
      tags: [quests]
      summary: List all available quests
      responses:
        "200":
          description: List of quests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Quest"

  /quests/{id}/claim:
    post:
      operationId: claimQuest
      tags: [quests]
      summary: Claim Trade Credits reward for completing a quest
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimQuestBody"
      responses:
        "200":
          description: Quest claimed, Trade Credits awarded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClaimQuestResponse"
        "400":
          description: Already claimed or quest not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /rewards/daily:
    post:
      operationId: claimDailyReward
      tags: [rewards]
      summary: Claim daily login reward in Trade Credits (streak-based)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DailyRewardBody"
      responses:
        "200":
          description: Daily reward claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyRewardResponse"
        "400":
          description: Already claimed today
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    RegisterUserBody:
      type: object
      properties:
        telegramId:
          type: string
        username:
          type: ["string", "null"]
        firstName:
          type: ["string", "null"]
        lastName:
          type: ["string", "null"]
        photoUrl:
          type: ["string", "null"]
        referredBy:
          type: ["string", "null"]
      required:
        - telegramId

    UpdateWalletBody:
      type: object
      properties:
        walletAddress:
          type: string
      required:
        - walletAddress

    UpgradeToVipBody:
      type: object
      properties:
        plan:
          type: string
          enum: [weekly, monthly, tc]
        txHash:
          type: ["string", "null"]
      required:
        - plan

    User:
      type: object
      properties:
        id:
          type: integer
        telegramId:
          type: string
        username:
          type: ["string", "null"]
        firstName:
          type: ["string", "null"]
        lastName:
          type: ["string", "null"]
        photoUrl:
          type: ["string", "null"]
        tradeCredits:
          type: integer
        goldCoins:
          type: integer
        totalGcEarned:
          type: integer
        isVip:
          type: boolean
        vipExpiresAt:
          type: ["string", "null"]
        vipTrialExpiresAt:
          type: ["string", "null"]
        hasVerified:
          type: boolean
        walletAddress:
          type: ["string", "null"]
        referredBy:
          type: ["string", "null"]
        loginStreak:
          type: integer
        lastLoginDate:
          type: ["string", "null"]
        registrationDate:
          type: ["string", "null"]
        dailyGcEarned:
          type: integer
        dailyGcDate:
          type: ["string", "null"]
        weeklyWithdrawnGc:
          type: integer
        createdAt:
          type: string
      required:
        - id
        - telegramId
        - tradeCredits
        - goldCoins
        - totalGcEarned
        - isVip
        - hasVerified
        - loginStreak
        - dailyGcEarned
        - weeklyWithdrawnGc
        - createdAt

    UserStats:
      type: object
      properties:
        totalPredictions:
          type: integer
        wins:
          type: integer
        losses:
          type: integer
        winRate:
          type: number
        totalTcWagered:
          type: integer
        totalGcEarned:
          type: integer
        referralCount:
          type: integer
        rank:
          type: integer
      required:
        - totalPredictions
        - wins
        - losses
        - winRate
        - totalTcWagered
        - totalGcEarned
        - referralCount
        - rank

    CreatePredictionBody:
      type: object
      properties:
        telegramId:
          type: string
        direction:
          type: string
          enum: [long, short]
        amount:
          type: integer
        entryPrice:
          type: number
      required:
        - telegramId
        - direction
        - amount
        - entryPrice

    ResolvePredictionBody:
      type: object
      properties:
        exitPrice:
          type: number
      required:
        - exitPrice

    Prediction:
      type: object
      properties:
        id:
          type: integer
        telegramId:
          type: string
        direction:
          type: string
        amount:
          type: integer
        entryPrice:
          type: number
        exitPrice:
          type: ["number", "null"]
        status:
          type: string
          enum: [pending, won, lost]
        payout:
          type: ["integer", "null"]
        createdAt:
          type: string
        resolvedAt:
          type: ["string", "null"]
      required:
        - id
        - telegramId
        - direction
        - amount
        - entryPrice
        - status
        - createdAt

    LeaderboardEntry:
      type: object
      properties:
        telegramId:
          type: string
        username:
          type: ["string", "null"]
        firstName:
          type: ["string", "null"]
        goldCoins:
          type: integer
        totalGcEarned:
          type: integer
        isVip:
          type: boolean
        rank:
          type: integer
      required:
        - telegramId
        - goldCoins
        - totalGcEarned
        - isVip
        - rank

    Quest:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        description:
          type: string
        reward:
          type: integer
        externalUrl:
          type: string
        category:
          type: string
        isVipOnly:
          type: boolean
        iconName:
          type: string
      required:
        - id
        - title
        - description
        - reward
        - externalUrl
        - category
        - isVipOnly
        - iconName

    ClaimQuestBody:
      type: object
      properties:
        telegramId:
          type: string
      required:
        - telegramId

    ClaimQuestResponse:
      type: object
      properties:
        tcAwarded:
          type: integer
        newTcBalance:
          type: integer
        message:
          type: string
      required:
        - tcAwarded
        - newTcBalance
        - message

    DailyRewardBody:
      type: object
      properties:
        telegramId:
          type: string
      required:
        - telegramId

    DailyRewardResponse:
      type: object
      properties:
        tcAwarded:
          type: integer
        newTcBalance:
          type: integer
        streak:
          type: integer
        message:
          type: string
        isVipBonus:
          type: boolean
      required:
        - tcAwarded
        - newTcBalance
        - streak
        - message
        - isVipBonus
